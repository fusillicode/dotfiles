#!/bin/bash

set -euo pipefail

# cargo bins ❤️
rustup component add rust-analyzer
cargo install --force \
  atuin bat broot cargo-make ccase drill git-delta pv ripgrep rtx-cli sd sqlx-cli
cargo install --force taplo-cli --all-features