#!/bin/bash

set -euo pipefail

# cargo bins ❤️
rustup component add rust-analyzer
cargo install --force \
  atuin bat cargo-make ccase drill pv ripgrep rtx-cli sd sqlx-cli
cargo install --force taplo-cli --all-features