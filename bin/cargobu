#!/bin/bash

set -euo pipefail

# cargo bins ❤️
rustup component add rust-analyzer
cargo install --force \
  atuin bacon bat cargo-make ccase drill git-delta pv repgrep ripgrep rtx-cli sd skim sqlx-cli taplo-cli
