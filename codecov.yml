# Codecov configuration for yog workspace with per-crate components (normalized paths)
# Reference: https://docs.codecov.com/docs/codecov-yaml
# Components configured via component_management (official syntax).
# Upload step runs with working-directory: yog, so paths include leading 'yog/'.

codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true

coverage:
  precision: 2
  round: down
  range: 60..90
  status:
    project:
      default:
        target: 29%
        threshold: 2%
        informational: true # Flip to false once baseline accepted.
    patch:
      default:
        target: 29%
        threshold: 2%
        informational: false
    changes:
      default:
        informational: true

ignore:
  - "yog/crates/nomicon/templates/"
  - "yog/crates/nomicon/assets/"
  - "**/build.rs"

comment:
  layout: "header, diff, flags, components"

# Component management (per-crate components).
component_management:
  default_rules:
    statuses:
      - type: project
        target: auto # Let Codecov determine target initially.
        informational: true # Make non-blocking until data stabilizes.
  individual_components:
    - component_id: catl
      name: catl
      paths:
        - yog/crates/catl/**
    - component_id: evoke
      name: evoke
      paths:
        - yog/crates/evoke/**
    - component_id: fkr
      name: fkr
      paths:
        - yog/crates/fkr/**
    - component_id: gch
      name: gch
      paths:
        - yog/crates/gch/**
    - component_id: gcu
      name: gcu
      paths:
        - yog/crates/gcu/**
    - component_id: ghl
      name: ghl
      paths:
        - yog/crates/ghl/**
    - component_id: idt
      name: idt
      paths:
        - yog/crates/idt/**
    - component_id: miskatonic
      name: miskatonic
      paths:
        - yog/crates/miskatonic/**
    - component_id: nomicon
      name: nomicon
      paths:
        - yog/crates/nomicon/**
    - component_id: nvrim
      name: nvrim
      paths:
        - yog/crates/nvrim/**
    - component_id: oe
      name: oe
      paths:
        - yog/crates/oe/**
    - component_id: rmr
      name: rmr
      paths:
        - yog/crates/rmr/**
    - component_id: tec
      name: tec
      paths:
        - yog/crates/tec/**
    - component_id: try
      name: try
      paths:
        - yog/crates/try/**
    - component_id: vpg
      name: vpg
      paths:
        - yog/crates/vpg/**
    - component_id: yghfl
      name: yghfl
      paths:
        - yog/crates/yghfl/**
    - component_id: yhfp
      name: yhfp
      paths:
        - yog/crates/yhfp/**
    - component_id: ytil_cmd
      name: ytil_cmd
      paths:
        - yog/crates/ytil/cmd/**
    - component_id: ytil_editor
      name: ytil_editor
      paths:
        - yog/crates/ytil/editor/**
    - component_id: ytil_git
      name: ytil_git
      paths:
        - yog/crates/ytil/git/**
    - component_id: ytil_github
      name: ytil_github
      paths:
        - yog/crates/ytil/github/**
    - component_id: ytil_hx
      name: ytil_hx
      paths:
        - yog/crates/ytil/hx/**
    - component_id: ytil_system
      name: ytil_system
      paths:
        - yog/crates/ytil/system/**
    - component_id: ytil_tui
      name: ytil_tui
      paths:
        - yog/crates/ytil/tui/**
    - component_id: ytil_wezterm
      name: ytil_wezterm
      paths:
        - yog/crates/ytil/wezterm/**
